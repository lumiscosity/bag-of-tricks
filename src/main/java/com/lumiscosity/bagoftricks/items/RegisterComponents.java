package com.lumiscosity.bagoftricks.items;

import com.mojang.serialization.Codec;
import net.minecraft.component.ComponentType;
import net.minecraft.network.codec.PacketCodecs;
import net.minecraft.registry.Registries;
import net.minecraft.registry.Registry;
import net.minecraft.util.Identifier;

public class RegisterComponents {
    // public static final ComponentType<Integer> BAG_LEVEL = ComponentType.builder().codec(Codec.INT).packetCodec(PacketCodecs.VAR_INT).build();

    // public static void initComponents() {
    //     Registry.register(Registries.DATA_COMPONENT_TYPE, Identifier.of("bagoftricks", "baglevel"), BAG_LEVEL);
    // }
}
